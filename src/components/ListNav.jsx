export const ListNav = () => {
  return (
    <nav className="list-section__nav">
      <button className="active">Top</button>
      <button>New</button>
      <button>Ask</button>
      <button>Show</button>
    </nav>
  )
}